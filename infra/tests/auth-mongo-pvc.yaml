apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: auth-mongo-data-pvc
spec:
  storageClassName: ""
  accessModes:
    - ReadWriteOnce
  volumeName: pv
  resources:
    requests:
      storage: 1Gi